<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Android 13+ notifications permission (runtime permission is requested in code) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Coarse only: compliance_service uses LocationAccuracy.low for state-level region gating. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- R1D7 voice input — speech_to_text needs mic; SpeechRecognizer also
         needs RECOGNIZE_SPEECH on some OEM builds. The package's setup docs
         require this queries block too so SpeechRecognizer can resolve the
         on-device recognizer service. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Required so scheduled notifications survive a reboot / app update.
         Paired with ScheduledNotificationBootReceiver below. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            
            <!-- Deep linking configuration -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <!-- Custom scheme -->
                <data android:scheme="gentlequest"/>

                <!-- App Links -->
                <data android:scheme="https"
                      android:host="gentlequest.com"/>
                <data android:scheme="https"
                      android:host="gentlequest.app"/>
            </intent-filter>

            <!-- App Links: https + /auth path-prefix.
                 With autoVerify="true" + a valid /.well-known/assetlinks.json
                 served from gentlequest.app, Android will route taps on
                 https://gentlequest.app/auth/... directly to this activity
                 (no chooser, no browser hop), which closes the same-device
                 hijack window where a malicious app registers gentlequest://.
                 Kept SEPARATE from the catch-all intent-filter above so the
                 path-prefix verification is unambiguous for Play Protect. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="gentlequest.app" android:pathPrefix="/auth" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_local_notifications scheduling receivers.
             LOAD-BEARING: without these, EVERY zonedSchedule notification is
             silently dropped on Android. zonedSchedule delivers via an
             AlarmManager PendingIntent.getBroadcast targeting
             ScheduledNotificationReceiver; if the class is not declared, the
             broadcast goes nowhere and nothing surfaces.

             The plugin does NOT declare these for you. Its own
             android/src/main/AndroidManifest.xml declares only VIBRATE and
             POST_NOTIFICATIONS. The receivers appear only in the plugin's
             example app, i.e. documentation of what a consuming app must add.
             Verified 2026-08-20 against plugin 17.2.4, and against this app's
             merged release manifest, which contained ZERO receivers.

             Consequence of their absence, in shipped builds: dailyCheckin,
             worriedCheckin, weeklyReview, streakNudge AND crisisFollowup were
             all undeliverable on Android. It went unnoticed because Settings'
             "Send a test notification" uses _plugin.show(), which fires
             immediately and bypasses this receiver entirely, so the test
             button worked while everything scheduled failed.

             Deliberately NOT adding SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM:
             all call sites use AndroidScheduleMode.inexactAllowWhileIdle, and
             exact-alarm permissions trigger Play policy review.

             See docs/NOTIFICATION_AND_RETENTION_FINDINGS_2026-08-20.md §1. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- speech_to_text — resolve on-device SpeechRecognizer service. -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
