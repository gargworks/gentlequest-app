// Central place to define the iOS bundle identifier
// Default value; override in local-overrides.xcconfig (not committed) if needed
APP_BUNDLE_ID = com.gentlequest.app

// Apple Developer Team ID (can be overridden in local-overrides.xcconfig)
DEVELOPMENT_TEAM = 828Q2S3G4Q
