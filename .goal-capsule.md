# GOAL CAPSULE

> Read this FIRST after any context reset (compaction, new session, resumed
> work). A summary carries facts -- files touched, commands run -- but not
> WEIGHT: what this work is actually for, and what to say no to. This file
> is the weight. Keep it current or it rots into a stale assertion.

## GOAL
Ship GentleQuest app updates safely — crisis detector, compliance, and user-facing features that actually reach production.

## CURRENT STATE
_Updated 2026-08-27, post-1.7.1 release + repo consolidation._
- **v1.7.1+26082701 is LIVE in Play production** and live on Play internal. It supersedes 1.7.0+26082201, which shipped with two accessibility defects on the PHQ-9 crisis screen (988 label clipping / pill below fold at large text scale — fixed by 645754bb).
- **iOS 1.7.1: submitted to App Store review** (`WAITING_FOR_REVIEW`, auto-releases on approval). TestFlight: internal AND external (Public Beta) both `IN_BETA_TESTING` — iOS testers have the crisis fix now. iOS build required manual signing (no Xcode account on this machine) and a MinimumOSVersion patch to App.framework; see git history 2026-08-27.
- **The clinical/legal gate was BYPASSED for this release by explicit operator decision** — see the 2026-08-27 amendment in `docs/REVIEW_PACKET_RUMINATION_v1.7.0.md`. The Loop Reset clinical review is now a post-release obligation, still outstanding.
- **Repo consolidated (2026-08-27):** stale/aspirational docs and ~15k lines of dead code (quest UI, dhiwise, leopard, orphans) moved to `archive/` — nothing deleted, all tombstoned in `archive/ARCHIVE_INDEX.md`; nucleus-flagged items listed in `archive/DEFERRED_DECISIONS.md`. `docs/INDEX.md` is now the canonical doc map. Test baseline is now **376 passed / 5 failed** (known: j03_compliance ×4, interactive_chat ×1); analyze baseline 8.
- `BILLION_DOLLAR_ROADMAP.md` is now TRACKED at `docs/strategy/` (was gitignored + uncommitted — one `git clean` from loss).
- Open crisis-routing decisions for the clinical reviewer: repo issues **#6** (crisis follow-up notification built but never called; 5min-vs-24h discrepancy) and **#7** (crisis deep link has no real destination; normalized to home as stopgap).
- Backend `intervention_outcome` endpoint accepts `exercise_type="rumination_reset"` without receiving free text.

## DECISIONS MADE
- Pushed `gentlequest` to GitLab because the GitHub CLI account is blocked and the stored GitHub token is invalid.
- Pushed `ai-mvp-backend` to a newly initialized local bare mirror because no GitLab project exists for it and no SSH key is authorized for GitHub.
- iOS `AppDelegate.swift` was reverted to the older standard pattern; it builds on the installed Flutter 3.32.8 SDK but may need revisiting when the SDK is upgraded to the version that introduced `FlutterImplicitEngineDelegate`.
- Focus contracts stay in advisory mode; the hard `PreToolUse` enforcement cap is deferred until real-world calibration data is available.

## POST-COMPACT RULE
A compaction summary carries facts, not weight. After any context reset,
re-read this capsule FIRST -- before looking at the pending list, before
resuming any task -- and re-anchor on GOAL. Then, for each pending item,
ask: does this serve the goal, or is it just the workbench? "Continue"
means continue the goal, not the ticket queue.

## REPORT BACK (survives compaction because this file does)
This capsule came from a kit built in a separate repo. If something about
it breaks, surprises you, or works well, report it -- this instruction is
written here, not just said once in chat, specifically so it survives the
next compaction the way a spoken instruction would not:
1. The flywheel (below) is the PRIMARY channel -- it needs no addressing
   and doesn't depend on any session being reachable. Use it for anything
   concrete: a false block, a bug, docs that didn't match reality.
2. SendMessage is SECONDARY and conditional: only if you're REPLYING to an
   incoming cross-session message about this kit, copy that message's
   "from" address as your "to" -- that resolves directly. Never cold-
   initiate SendMessage to a bare name ("main", "peer") or to any id baked
   into this file -- both were tried live and both failed (dozens of
   stale same-named sessions; a hardcoded id goes dead the moment that
   session ends). If you have no inbound message to reply to, skip
   SendMessage entirely and use the flywheel only.
   python3 -c "
   import sys; sys.path.insert(0, '$HOME/ai-mvp-backend')
   from mcp_server_nucleus.flywheel import Flywheel
   from pathlib import Path
   fw = Flywheel(Path.home() / 'ai-mvp-backend' / '.brain')
   fw.file_ticket(step='<short-slug>', error='<what happened>', logs='<exact command + output>', phase='goal_capsule_kit_field_use')
   "
Don't invent friction to report. Silence -- nothing broke, nothing worth
saying -- is a valid, honest outcome too, and needs no report at all.
