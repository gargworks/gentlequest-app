# 🎬 Nucleus v0.5 "The Cracken" Production Protocol

This document outlines the exact steps to record the Phase C "Netscape Event" demo and synchronize it with the high-fidelity v0.5 voiceovers.

---

## 🛠️ Step 1: Terminal Prep (Clean Slate)
To ensure Phase C discovery works flawlessly on camera, run this sequence in your terminal first:

```bash
# 1. Clear existing demo brains to avoid context rot
rm -rf ~/.demo_brain_v0.5

# 2. Set the environment for the demo session
export NUCLEAR_BRAIN_PATH="/Users/lokeshgarg/.demo_brain_v0.5"

# 3. Initialize the demo structure
mkdir -p $NUCLEAR_BRAIN_PATH/ledger
```

---

## 🖥️ Step 2: Claude Desktop Configuration
Verify your `~/Library/Application Support/Claude/claude_desktop_config.json` is pointing to the v0.5 source:

```json
{
  "mcpServers": {
    "nucleus": {
      "command": "python3",
      "args": [
        "/Users/lokeshgarg/ai-mvp-backend/mcp-server-nucleus/src/mcp_server_nucleus/stdio_server.py"
      ],
      "env": {
        "NUCLEAR_BRAIN_PATH": "/Users/lokeshgarg/.demo_brain_v0.5",
        "PYTHONPATH": "/Users/lokeshgarg/ai-mvp-backend/mcp-server-nucleus/src"
      }
    }
  }
}
```

---

## 🎬 Step 3: Recording Sequence (Phase C)

### Visual Flow (Loom/QuickTime)
1. **Terminal (Left)**: Run the "Thanos Snap" script.
   ```bash
   python3 /Users/lokeshgarg/ai-mvp-backend/mcp-server-nucleus/scripts/demo_c_recursive_mounting.py
   ```
   *Action: Show it mounting Stripe, Postgres, and Brave Search instantly.*

2. **Claude Desktop (Right)**:
   - Ask: "What tools are currently mounted in my mesh?"
   - *Result: Agent shows the virtualized tools (stripe:list_customers, postgres:query).*
   - Ask: "List my Stripe customers."
   - *Result: Nucleus routes the call, government ledger logs it, success returned.*

### Dual-Track Narration Scripts

#### Option 1: The Master Pitch (Long-form, 2:00+)
> "Welcome to the Netscape moment for the Agentic Web. Today, we solve fragmentation."
>
> "You're seeing the Thanos Snap in action. Instead of configuring 50 tools, we mount one parent Nucleus. Instantly, our agent inherits a unified namespace of every child server below it—recursive discovery at fractal scale."
>
> "We're invoking Stripe and Postgres through a single, governed gate. This isn't just connectivity; it's the Verisign Pillar. Every call generates a cryptographic receipt, ensuring that in the age of autonomy, trust is infrastructure, not just a prompt."

#### Option 2: The Social Snippet (Short-form, 45s)
> "Linear tool growth is a trap. Nucleus v0.5 changes the math."
> 
> "One connection to Nucleus provides a unified interface to your entire tool mesh. Recursive mounting, virtual namespacing, and total control."
> 
> "The browser for agents is here. pip install nucleus-mcp."

---

## 🎞️ Step 4: Assembly
Once recorded, use the assembly script to sync with the "Antoni" voiceover:

```bash
cd ~/ai-mvp-backend
./scripts/assemble_demo.sh \
  output/v0.5_demos/demo_c_screen.mov \
  output/v0.5_demos/demo_c_voiceover.mp3 \
  output/v0.5_demos/demo_c_final.mp4
```

---

## 🐙 Strategic Checklist
- [ ] **Phase A (Lock)**: Recorded (2:00 Long / 30s Short)
- [ ] **Phase B (Engram)**: Recorded (2:00 Long / 30s Short)
- [ ] **Phase C (Netscape)**: 🟢 READY FOR RECORDING
