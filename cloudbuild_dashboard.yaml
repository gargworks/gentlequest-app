steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/${PROJECT_ID}/marketing-dashboard', '-f', 'Dockerfile.dashboard', '.']
images:
- 'gcr.io/${PROJECT_ID}/marketing-dashboard'
